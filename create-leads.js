const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function createLeads() { const company = await prisma.company.findFirst(); const leads = await prisma.lead.createMany({ data: [{ firstName: 'Walther', lastName: 'Alcocer Gutierrez', email: 'walther.alcocer@example.com', phoneNumber: '+51982014786', companyId: company.id }, { firstName: 'Juan', lastName: 'Pérez', email: 'juan.perez@example.com', phoneNumber: '+51999888777', companyId: company.id }, { firstName: 'María', lastName: 'García', email: 'maria.garcia@example.com', phoneNumber: '+51966555444', companyId: company.id }] }); console.log('Leads creados:', leads.count); await prisma.(); } createLeads();
